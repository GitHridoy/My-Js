var Mobile = {
    Model: 'I75',
    Ram: '2GB',
    CPU: 'MTK',
    Price: 2400,
    
}

var finde = Object.keys(Mobile);

for(var is = 0; is < finde.length; is++){
    var Total = finde[is];
    console.log(Total)
}
